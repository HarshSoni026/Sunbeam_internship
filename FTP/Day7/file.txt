Study:

- Read opendir(), readdir(), closedir() from <dirent.h>.
- How directory listing is used in FTP’s LIST command.


Task:

Task 1 – Filter only files or only directories
Hint: Use dir->d_type:

Task 2 – Accept directory path as command-line argument
